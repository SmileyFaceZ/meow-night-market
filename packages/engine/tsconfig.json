{
  // Tests + tooling config. The engine source itself is also checked by tsconfig.src.json.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["src", "test", "scripts", "vitest.config.ts"]
}
