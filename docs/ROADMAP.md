# Roadmap — ทำทีละเฟส หยุดให้ผู้ใช้ตรวจทุกครั้งที่จบเฟส

## เฟส 0 — ตั้งโปรเจกต์
- [x] monorepo + TypeScript strict + ESLint + Prettier + Vitest
- [x] Vite + React + Tailwind + ฟอนต์ Mitr / Noto Sans Thai
- [x] ตั้ง CSS variables ตาม ART_DIRECTION.md
- [x] สร้าง `docs/DECISIONS.md` และบันทึกการตัดสินใจแรก
- [x] อัปเดตคำสั่งใน CLAUDE.md ให้ตรงของจริง

## เฟส 1 — Game engine (ยังไม่มี UI)
- [x] types, config, seeded RNG, createGame
- [x] 3 ช่วงของรอบ + ทิ้งการ์ดเกิน + จบเกม + นับแต้ม
- [x] getPlayerView ซ่อนข้อมูลถูกต้อง
- [x] unit test ครบทุกกติกา + property test 1,000 เกม + determinism test
- [x] script จำลองเกมใน terminal ให้ผู้ใช้ลองดู log

## เฟส 2 — บอท
- [x] บอท 3 นิสัย + ระดับง่าย/ปกติ
- [x] script สถิติสมดุล → รายงานผลและเสนอปรับค่าใน config ถ้าจำเป็น (`docs/BALANCE.md` — ใช้กติกา D+E+R+S+Xt+P)

## เฟส 3 — UI โหมดเดี่ยว (เล่นได้จบเกม)
- [x] หน้า Home / ตั้งค่าเกม / เกม / ผลลัพธ์
- [x] การ์ด SVG ครบทุกชนิด (เวอร์ชันแรก เรียบร้อยพอใช้)
- [x] แถบบอกช่วงเกม + ปุ่มแอคชันที่ชัดเจน
- [x] i18n ไทย/อังกฤษ ทุกข้อความ
- [x] บันทึกและเล่นต่อได้

## เฟส 4 — ความน่ารักและความรู้สึก
- [ ] ขัดเกลาภาพการ์ด แมว หมา ฉากตลาด ตาม ART_DIRECTION.md (ทำบางส่วน: สีหน้าแมว 4 แบบ, ถังขยะ 4 อารมณ์, ป้าเหมียว — ยังไม่ขัดเกลาภาพการ์ดและฉากตลาด)
- [x] แอนิเมชันทุกเหตุการณ์หลัก + ข้ามได้ + reduced motion
- [x] บทสอนแบบโต้ตอบ (เกมแรก เซ็ตการ์ดตายตัว)
- [x] หน้าวิธีเล่นแบบภาพ
- [x] ทดสอบบนจอ 375px และเดสก์ท็อป (+ โต๊ะ 3 คอลัมน์บนจอ ≥ 1024px)

## เฟส 5 — เล่นเครื่องเดียว (pass-and-play)
- [x] หน้าจอบังมือ "ส่งเครื่องให้ {{name}}" ระหว่างตา
- [x] ช่วงประมูลให้เลือกทีละคนแบบลับ
- [x] หน้าตั้งค่าเล่นเครื่องเดียว (คน 2–4 คน + เติมบอทได้) · เล่นต่อหลังปิดเว็บได้

## เฟส 6 — เล่นกับเพื่อนออนไลน์
- [x] Durable Object GameRoom + protocol + zod
- [x] Lobby, รหัสห้อง, ลิงก์แชร์, เติมบอท
- [x] reconnect, ตัวจับเวลา, บอทแทนคนหลุด
- [x] สติกเกอร์แมว
- [x] Playwright test 2 ผู้เล่นจนจบเกม

## เฟส 7 — ปล่อยเกม
- [x] Worker ตัวเดียวเสิร์ฟหน้าเว็บ + server, deploy อัตโนมัติด้วย Workers Builds (ตั้งค่าใน repo แล้ว)
- [ ] deploy จริง — รอผู้ใช้ทำขั้นตอนในหน้า Cloudflare ตาม `docs/DEPLOY.md`
- [x] `docs/DEPLOY.md` ภาษาไทย + ประเมินโควตาแพ็กเกจฟรี (`npm run usage`)
- [ ] meta tags + รูปพรีวิวตอนแชร์ลิงก์ (Open Graph) เป็น SVG→PNG ที่สร้างเอง
- [ ] favicon แมว, PWA ติดตั้งได้
- [ ] หน้า "เกี่ยวกับ" + เครดิต

## ส่วนขยาย v2 — "ตลาดป่วน" (GAME_RULES §12–15, ผู้ใช้ยืนยันกติกาแล้ว)
- [x] ขั้น 1: เล่นอีกรอบ — ออนไลน์ (พร้อม/ดูข้างสนาม/สกอร์ห้อง/ห้องค้าง 10 นาที) + เดี่ยว/เครื่องเดียว (สกอร์ต่อเนื่อง, เปลี่ยนการตั้งค่า)
- [x] ขั้น 2: พลังแมว ใน engine + เทสต์ทุกพลัง + บอทใช้พลัง + balance (รวมทางเลือก "เก็บของเก่ง" ของผู้ใช้)
  — แมวนำโชคปรับเป็น เสียครึ่งถุง+จบตา (DECISIONS 046)
- [x] ขั้น 3: การ์ดเหตุการณ์ ใน engine + รายงานสมดุลต่อใบ — ปรับฝนตก/รถขยะ/หมาขี้เซา และจมูกไว (DECISIONS 048)
- งาน v2 อยู่บน branch `v2-market-mayhem` — merge เข้า main หลังขั้น 4–5 และผู้ใช้ลองเล่นแล้ว
- [ ] ขั้น 4: ภาพ/หน้าจอ — แมวใหม่ 4 ตัว, ไอคอนพลัง 8, การ์ดเหตุการณ์ + ฉาก, เลือกแมว (ห้ามซ้ำ), สวิตช์โหมด
- [ ] ขั้น 5: คำอธิบายตลาดป่วน 2 หน้า + แท็บในหน้าวิธีเล่น

## เฟส 8 — ของแถม (ทำเมื่อผู้ใช้ขอ)
- [ ] ท้าทายรายวัน + สถิติส่วนตัว
- [x] เสียงประกอบ + เสียงกดปุ่ม + ตั้งค่าเสียง (Web Audio สังเคราะห์)
- [ ] สะสมสกินแมว/ฉากตลาด ปลดล็อกจากการเล่น
- [ ] โหมดสี high-contrast
