# Decisions Log

## 001 — Server-authoritative multiplayer on Cloudflare Durable Objects
เหตุผล: เกมมีข้อมูลลับ (การ์ดในมือ, การประมูลพร้อมกัน) จึงต้องให้ server ถือ state จริงและส่ง view แยกรายคน
Durable Object เหมาะกับ "1 ห้อง = 1 instance" และมี free tier

## 002 — Engine แยกเป็น package บริสุทธิ์ ใช้ร่วม client/server
เหตุผล: ทดสอบง่าย, deterministic, บอทใช้ได้ทั้งสองฝั่ง, โหมดเดี่ยวเล่นออฟไลน์ได้

## 003 — การ์ดวาดเป็น SVG component ทั้งหมด
เหตุผล: คมทุกขนาดจอ, ไฟล์เล็ก, เปลี่ยนสีตามธีมได้, ไม่มีปัญหาลิขสิทธิ์

## 004 — เวอร์ชันเครื่องมือ: TypeScript 6.0 และ Vitest 4.1 (ยังไม่ใช้ TS 7 / Vitest 5)
วันที่: 2026-09-24 (เฟส 0)
เหตุผล: `typescript-eslint` 8.70 รองรับ TypeScript `<6.1` เท่านั้น (TS 7 ตัวใหม่ที่เขียนด้วย Go ยังไม่มี API ให้ ESLint ใช้)
และ Vitest 5 ระบุรองรับ Node 22/24/26 แต่ไม่รวม Node 25 ที่เครื่องนี้ใช้ จึงเลือก Vitest 4.1 ที่รองรับ Node ≥ 24
ส่วนอื่นใช้ล่าสุด: Vite 8, React 19, Tailwind 4, ESLint 10 (flat config), i18next 26
เมื่อ typescript-eslint รองรับ TS 7 แล้วค่อยอัปเกรด

## 005 — แพ็กเกจภายในใช้เป็นซอร์ส TypeScript ไม่ต้อง build
เหตุผล: Vite (client) และ Wrangler/esbuild (server) คอมไพล์ TS ได้เอง จึงให้ `@meow/engine` ชี้ `exports` ไปที่ `src/index.ts`
ลดขั้นตอน build/watch และไม่มีปัญหา dist เก่าค้าง

## 006 — บังคับกฎ "engine บริสุทธิ์" ด้วยเครื่องมือ ไม่ใช่แค่ข้อตกลง
เหตุผล: กฎข้อ 1 ใน CLAUDE.md สำคัญต่อ determinism และการใช้ร่วม client/server
- `tsconfig.src.json` ของ engine ไม่มี lib DOM และไม่มี Node types → ใช้ `document`/`process` แล้ว typecheck ไม่ผ่าน
- ESLint ใน `packages/engine/src` ห้าม `Math.random`, `Date`, `window`/`document`/`fetch`/timers ฯลฯ และห้าม import `react`, `node:*`

## 007 — ห้ามข้อความตรงๆ ใน JSX ด้วยกฎ ESLint
เหตุผล: กฎข้อ 4 ใน CLAUDE.md — ใช้ `no-restricted-syntax` จับ JSXText ที่มีตัวอักษรไทย/อังกฤษ (ไม่ต้องเพิ่มปลั๊กอิน)
ข้อจำกัด: ไม่ครอบคลุมข้อความใน attribute เช่น `aria-label` ต้องตรวจตอนรีวิว

## 008 — ฟอนต์โฮสต์เองผ่าน @fontsource แทนการลิงก์ Google Fonts
เหตุผล: เป็นไฟล์ฟอนต์ชุดเดียวกับ Google Fonts แต่ bundle มากับเว็บ → เล่นออฟไลน์ได้เมื่อเป็น PWA,
ไม่ส่งคำขอไปบุคคลที่สาม, ไม่มีช่วงฟอนต์กระพริบจาก CDN ช้า — ใช้ Mitr 400/500/600 และ Noto Sans Thai (variable)
เบราว์เซอร์โหลดเฉพาะ subset ที่ใช้ (thai/latin) ตาม `unicode-range`

## 009 — i18n ใช้ i18next + react-i18next
เหตุผล: รองรับ `{{ตัวแปร}}` ตรงตามรูปแบบใน I18N.md และ plural ของอังกฤษผ่าน `Intl.PluralRules` ในตัว
ภาษาเริ่มต้น: ค่าใน localStorage (`mnm.lang`) → ถ้าไม่มี ใช้ไทย เว้นแต่เบราว์เซอร์ตั้งอังกฤษมาก่อนไทย
มี test ตรวจว่า th/en มีคีย์ตรงกัน ไม่มีค่าว่าง ตัวแปร `{{}}` ตรงกัน และคำในอภิธานศัพท์ตรงกับ I18N.md

## 010 — Design tokens เป็น CSS variables แล้ว map เข้า Tailwind ด้วย `@theme inline`
เหตุผล: SVG art อ้าง `var(--fish)` ได้ตรงๆ (เปลี่ยนธีมได้ภายหลัง เช่น high-contrast) และ component ใช้ utility `bg-fish` ได้
โดยค่ามีที่เดียวใน `apps/web/src/styles/index.css`

## 011 — `npm run lint` = ESLint + Prettier check
เหตุผล: ให้ "lint ผ่าน" ครอบคลุมรูปแบบโค้ดด้วย ใช้ `npm run format` แก้อัตโนมัติ ไม่จัดรูปแบบไฟล์ `.md` (เอกสารเขียนมือ มีตารางภาษาไทย)

## 012 — ผู้เริ่มรอบเวียนตามเข็มทุกรอบ (เปลี่ยนกติกาตามที่ผู้ใช้ตัดสิน) — ~~ยกเลิก~~ ดู 021
วันที่: 2026-09-24 (ก่อนเฟส 1)
เดิม: สุ่มผู้เริ่มเกมครั้งเดียว ใช้ตัดสินลำดับเมื่อลงเลขเหมียวเท่ากันตลอดเกม
ใหม่: สุ่มผู้เริ่มรอบแรก แล้วเลื่อนไปที่นั่งถัดไปทุกรอบ ใช้ตัดสินเฉพาะกรณีเลขเท่ากันเหมือนเดิม และ UI แสดงไอคอนผู้เริ่มรอบนี้
เหตุผล: การได้เล่นก่อนในช่วงกินได้เปรียบ (ราคายังสูง) ถ้าไม่เวียน คนเดิมจะได้เปรียบทุกครั้งที่ชนกัน
คำตอบอื่นที่ยืนยันพร้อมกัน (บันทึกใน GAME_RULES.md แล้ว): ลำดับช่วงคุ้ย/กินเรียงทุกคนตามเลขจากน้อยไปมาก,
ราคาตลาดไม่รีเซ็ต, รอบสุดท้ายไม่ต้องทิ้งการ์ดเกิน, กระดูกนับเป็นการ์ดในมือ, กองทิ้งเปิดทั้งกอง,
ถังเหลือแต่หมาและกองทิ้งว่าง = คุ้ยไม่ได้, ทิ้งการ์ดเกินเลือกพร้อมกันแบบลับ

## 013 — รูปแบบ API ของ engine
วันที่: 2026-09-24 (เฟส 1)
- `applyAction` คืน `{ ok: true, state, events }` หรือ `{ ok: false, error }` — error เป็น i18n key `error.*` (รายการอยู่ใน `ERROR_KEYS`)
  จะเพิ่มคำแปลใน th/en ตอนเฟส 3
- ภายใน `applyAction` โคลน state ด้วย JSON แล้วแก้บนสำเนา (state เป็นข้อมูล JSON ล้วนอยู่แล้วเพราะต้องเซฟ/ส่งผ่านเน็ต)
  โค้ดอ่านง่ายกว่าการ spread ทุกชั้น และ test ยืนยันว่า input ไม่ถูกแก้ (deep-freeze) — 1,000 เกมใช้เวลา ~7 วินาที
- การ์ดทุกใบมี `id` ไม่ซ้ำ เพื่อให้ตรวจการ์ดครบ 49 ใบได้ และใช้เป็น React key / อ้างถึงในแอคชัน
- `getPlayerView(state, null)` = มุมมองผู้ชม (ไม่มีมือของใคร) เตรียมไว้สำหรับผู้ชมในห้องออนไลน์
- `trashDogCount` เปิดเผยใน view เพราะหมาทุกตัวกลับเข้าถังเสมอ (คำนวณจากข้อมูลเปิดได้อยู่แล้ว) — บอทใช้คิดความเสี่ยง

## 014 — engine ไม่ข้ามตาให้อัตโนมัติ — ~~ยกเลิก~~ ดู 022
เหตุผล: ถ้า engine ข้ามตากินให้คนที่ไม่มีชุดกินได้ คนอื่นจะรู้ว่ามือเขาไม่มีชุด = ข้อมูลลับรั่ว
จึงต้องกด "กินเสร็จ" เองเสมอ (UI บอกใบ้ได้เฉพาะเจ้าของมือ) · ยกเว้นกรณีที่ไม่มีทางเลือกจริงและไม่เปิดเผยอะไร:
โดนหมาโดยไม่มีกระดูกจะโดนไล่ทันที (จำนวนกระดูกในมือคำนวณจากข้อมูลเปิดได้)
รอบ 5 ที่เหลือเลขเหมียวใบเดียว engine ยังรอแอคชัน `bid` ตามปกติ — ตัวควบคุมฝั่ง UI ส่งให้อัตโนมัติได้

## 015 — รายละเอียดการสับกองถังขยะ
- หมาที่จั่วได้ → ใส่กลับแล้วสับทั้งกอง (ตามกติกา "สับกลับ")
- เช็ก "ถังเหลือแต่หมา" ตอนจะคุ้ย (lazy) ถ้าเหลือแต่หมาแล้วกองทิ้งมีการ์ด → สับกองทิ้งทั้งหมดรวมกับหมาเป็นถังใหม่ แล้วจั่วต่อทันที
- ใช้ "ใบบนสุด" = ตำแหน่งแรกของ array

## 016 — ไฟล์ TS import ด้วยนามสกุล `.ts` + `erasableSyntaxOnly`
เหตุผล: Node ≥ 24 รัน `.ts` ได้เองโดยตัด type ทิ้ง จึงรันสคริปต์ (`npm run simulate`, สคริปต์สถิติเฟส 2) ด้วย `node` ตรงๆ
ไม่ต้องเพิ่ม tsx/ts-node · `erasableSyntaxOnly` กันการใช้ enum/namespace ที่ Node รันไม่ได้ · Vite และ esbuild รองรับ `.ts` import อยู่แล้ว

## 017 — บอทสุ่ม (`chooseRandomAction`) สำหรับทดสอบ
ไม่ใช่นิสัยบอทในเกม ใช้ขับ property test 1,000 เกม และสคริปต์ simulate ของเฟส 1 — ใช้แค่ PlayerView เหมือนบอทจริง

## 018 — จำนวนกระดูกในมือ และประวัติการ์ดที่หยิบจากแผง เป็นข้อมูลเปิด — รวมเข้ากับ 022 แล้ว

## 019 — การตีความนิสัยบอท (GAME_RULES §9)
- ความเสี่ยงเจอหมา = หมาในถัง ÷ การ์ดในถัง (ถ้าถังเหลือแต่หมา คิดรวมกองทิ้งที่จะถูกสับเข้ามา)
- **greedy**: ลงเลขสูงสุด (70%) หรือสูงรองลงมา (30%) · คุ้ยจนความเสี่ยง > 35% · กินมื้อที่ได้แต้มมากสุดทันที
- **sly**: ถ้ามีคนนำแต้ม เดาว่าจะลงเลขสูงสุดที่เหลือ แล้วลงชน (70%) · ไม่งั้นลงเลขที่ไม่มีใครเหลือ (ชนไม่ได้แน่นอน)
  · หยิบแผงให้น้ำหนักเพิ่มกับอาหารที่คู่แข่งสะสม · คุ้ยจนความเสี่ยง > 27% · โยนกระดูกเมื่อในถุงมี ≥ 2 ใบ
  · กินชนิดที่คู่แข่งเคยหยิบก่อนเสมอ ที่เหลือรอแบบ careful
- **careful**: ลงเลขต่ำสุด (70%) หรือต่ำรองลงมา (30%) · รอบ 2 ขึ้นไปถ้าแผงมีการ์ดที่ทำให้ครบมื้อได้ ลงเลขสูงสุด
  · คุ้ยจนความเสี่ยง > 20% (ถ้ามีกระดูก > 35%) · กินมื้อใหญ่ทันที มื้อปกติรอจนราคา ≤ 3 หรือมือใกล้เต็ม
- ทุกตัว: รอบสุดท้ายกินทุกอย่างที่กินได้ · ทิ้งการ์ดที่มีค่าน้อยที่สุด · ประเมินค่าการ์ดจากความใกล้ครบชุด × ราคา
- การสุ่ม 30% ในการลงเลขใส่ไว้เพราะบอทที่ลงเลขตายตัวทำให้เกมบอทนิสัยเดียวกันชนกัน 100% (สถิติเพี้ยน และคนเล่นจับทางได้)
- ระดับง่าย: 30% ของการตัดสินใจใช้บอทสุ่ม (`chooseRandomAction`)

## 020 — ตัวเลือกปรับสมดุลแบบทดลองใน config (ยังไม่ใช่กติกา) — ~~ยกเลิก~~ ดู 023
เพื่อจำลองทางเลือกให้ผู้ใช้ตัดสิน เพิ่ม `foodTypes`, `leftoverMarketToTrash`, `clashConsolationDraws`,
`clashedPickLast`, `recycleDiscardEachRound`, `clashTieBreak` ใน `GameConfig` — ค่าเริ่มต้นตรงกับกติกาปัจจุบันทุกข้อ (มี test ยืนยัน)
เพิ่ม event `CLASH_DRAW` และช่อง `to` ใน `MARKET_CLEARED` · หลังผู้ใช้เลือกแล้ว: ตัวที่เลือกย้ายเข้า GAME_RULES
ตัวที่ไม่เลือกลบออกจาก engine

## 021 — กติกาชุด B + D + E + R (ผู้ใช้เลือกจากรายงานสมดุลเฟส 2) — B ถูกตัดใน 025
วันที่: 2026-09-24
- **B** คนที่ชนกันจั่วถังขยะฟรี 1 ใบ (`CLASH_FREE_DRAWS`) หลังเลือกของจากแผง — ได้หมาไม่เสียอะไร หมากลับเข้าถัง
- **D** เริ่มช่วงคุ้ยทุกรอบ สับกองทิ้งทั้งหมดกลับเข้าถัง (แก้ "ถังตัน")
- **E** คนที่ชนกันยังได้เลือกของจากแผง ต่อจากคนที่ไม่ชน
- **R (ปรับตามผู้ใช้)** สุ่ม "ลำดับตัดสินเสมอ" ตอน **ต้นรอบ** (seeded) และแสดงให้ทุกคนเห็นก่อนลงเลข เพื่อวางแผนได้
  → **ยกเลิกผู้เริ่มรอบเวียน (012)** เหตุผล: 5 รอบหารจำนวนผู้เล่นไม่ลงตัว ผู้เริ่มเกมได้เริ่มรอบ 2 ครั้ง
  ทำให้ชนะ 29.7% จากค่ายุติธรรม 25% ที่ 4 คน (อัตราชนะไล่ตามที่นั่ง 29.7/25.7/23.2/21.3%) — การสุ่มใหม่ทุกรอบแก้ได้หมด
- ผลคือ state ไม่มี `firstStartSeat` แล้ว มี `tieOrder` แทน (อยู่ใน view และ event `ROUND_STARTED`)
- ยอมรับ 1.88 มื้อ/คน/เกมที่ 4 คน (ต่ำกว่าเป้า 2 เล็กน้อย) รอดูผลจากการเล่นจริง

## 022 — มือทุกคนเป็นข้อมูลเปิด + ข้ามตากินอัตโนมัติ (แทน 014 และรวม 018)
- การ์ดทุกใบเข้า/ออกจากมือต่อหน้าทุกคนอยู่แล้ว (แผง, ถุง, จั่วฟรี, กิน, ทิ้ง) คนความจำดีจึงรู้มือทุกคนได้ครบ
  ผู้ใช้เลือกเปิดตรงๆ ให้ยุติธรรม · `PublicPlayer.hand` มีการ์ดของทุกคน
- ยังลับ: เลขเหมียวก่อนเปิดพร้อมกัน, ลำดับการ์ดในกอง (marketDeck, trashDeck), การ์ดที่เลือกทิ้งตอนมือเกินก่อนเปิดพร้อมกัน
- ลบ `boneCount` และ `picks` ที่เพิ่มใน 018 ออก (ซ้ำซ้อนเมื่อมือเปิดแล้ว) — บอท sly ดูมือคู่แข่งตรงๆ แทน
- ช่วงกิน: ไม่มีชุดที่กินได้ตอนเริ่มตา → ข้ามตา (event `TURN_SKIPPED`) · กินชุดสุดท้ายที่กินได้แล้ว → จบตาเอง
  (ผู้ใช้ระบุว่าแทน DECISIONS 013 — เรื่องนี้จริงๆ อยู่ในข้อ 014 จึงบันทึกว่าแทน 014)
- CLAUDE.md กฎข้อ 3 ปรับให้ตรง: ห้ามส่งลำดับกองและการเลือกลับก่อนเปิด (มือไม่ใช่ความลับแล้ว)

## 023 — ลบสวิตช์ทดลองออกจาก engine (แทน 020)
D, E, R กลายเป็นกติกาถาวร (ไม่มีสวิตช์) · B เหลือเป็นค่าตัวเลข `clashFreeDraws` (ตั้ง 0 = ไม่มี B) เพื่อจำลองเทียบได้
ลบ `foodTypes`, `leftoverMarketToTrash`, `clashTieBreak` และ event field `MARKET_CLEARED.to`

## 024 — ปรับเกณฑ์บอทให้อัตราชนะใกล้กัน (ระดับปกติ)
- รวมทุกเกณฑ์ไว้ที่ `bots/tuning.ts` (ย้ายไป `config.ts` ใน 029) · ปรับเฉพาะเกณฑ์ความเสี่ยงและจังหวะตามที่ผู้ใช้กำหนด
- sweep ด้วยสคริปต์ทีละชุดค่า แล้วยืนยันด้วย `npm run balance` 1,000 เกม/ไลน์อัป
  (sweep 60–100 เกม/ไลน์อัปพลาดได้ ~5 จุด — ค่าที่ดูดีตอน sweep เล็ก ห่าง 15 จุดตอนรันเต็ม)
- ค่าสุดท้าย: greedy คุ้ยถึง 28% หรือถุง 6 ใบ (เดิม 35%) · sly 24% (เดิม 27%) · careful 19% หรือถุง 5 ใบ (เดิม 20%, มีกระดูก 35%)
- เพิ่มเกณฑ์ "ขนาดถุง": ที่ 2 คนถังใหญ่ ความเสี่ยงต่อใบต่ำ บอทจึงคุ้ยยาวจนถุงใหญ่แล้วโดนไล่เสียหมด —
  หยุดเมื่อถุงเต็มพอเป็นเกณฑ์แบบ push-your-luck ที่คนเล่นจริงใช้ ไม่ทำให้บอทดูโง่
- careful ยังรอมื้อใหญ่ (กินมื้อปกติทันทีเมื่อราคา ≤ 3) — การเลิกรอช่วยสมดุลได้บ้างแต่ทำให้นิสัยหาย จึงไม่ใช้
- careful ไวต่อเกณฑ์มากที่ 4 คน (ถังเล็ก): 17% → แทบไม่คุ้ย ชนะแค่ 13%
- บอทใช้มือที่เปิดแล้ว: sly กินชนิดที่คู่แข่ง (ที่จะกินทีหลังในรอบนี้) กินได้ก่อน และแย่งการ์ดที่จะทำให้คู่แข่งครบชุด
  careful/sly ไม่รอมื้อใหญ่ถ้าคู่แข่งที่กินทีหลังจะกินชนิดเดียวกันได้ (ราคาจะลดก่อน)
- ผล (1,000 เกม/ไลน์อัป, กติกา B+D+E+R): ห่างสุด 4.2 / 1.3 / 4.1 จุด ที่ 2 / 3 / 4 คน

## 025 — ตัด B (จั่วฟรีเมื่อชน) ออกจากกติกา เหลือ D + E + R
วันที่: 2026-09-24 (เฟส 2 รอบ 3)
เหตุผล: รายงานสมดุลพบว่าคนที่ชนได้การ์ดมากกว่าคนที่ไม่ชน (3 คน 2.71 vs 2.12 ใบ/รอบ) และได้การ์ดที่ได้กินจริงมากกว่า
ซึ่งผิดเจตนาของเกมประมูล — ตัด B แล้วทุกคนได้จากแผงคนละ 1 ใบเท่ากัน คนไม่ชนได้เปรียบจากการเลือกก่อน
(ได้กินจริงมากกว่า 8% ที่ 3 คน, 24% ที่ 4 คน) · ลบ `clashFreeDraws` และ event `CLASH_DRAW` ออกจาก engine

## 026 — ตัวเลือกทดลองรอบ 3: M (ช่วงเลขเหมียวตามจำนวนผู้เล่น) และ S (ลำดับตัดสินเสมอตามแต้ม) — ผลใน 027
- ยังไม่ใช่กติกา รอผู้ใช้เลือกจากรายงาน · ค่าเริ่มต้นตรงกับกติกาปัจจุบัน
- M: engine รองรับ `meowValues` ยาวกว่าจำนวนรอบได้แล้ว (เลขที่เหลือไม่ได้ใช้) — สคริปต์ใส่ 1–5 / 1–6 / 1–7 ตามจำนวนผู้เล่น
- S: `tieOrderByScore` — รอบ 1 สุ่ม รอบ 2+ เรียงแต้มน้อยก่อน แต้มเท่ากันใช้ลำดับที่สุ่มตอนต้นรอบ
- บอทรองรับช่วงเลขยาว: careful ปกติลงเลขต่ำ ถ้าเหลือเลขเกินจำนวนรอบ ใน 2 รอบสุดท้าย (`highBidLastRounds`) ลงเลขสูงสุด
  ไม่งั้นจะจบเกมโดยถือ 6–7 ไว้เปล่าๆ · ค่าคงที่ที่ยังฝังในโค้ดบอท (น้ำหนักกระดูกตอนทิ้ง, น้ำหนักแย่งการ์ด) ย้ายไป `tuning.ts` หมดแล้ว
- เพิ่มระหว่างทาง (ผมเสนอ): **X** `tieReverse` — เลขเท่ากัน เลือกของตามลำดับตัดสินเสมอ แต่คุ้ย (และ/หรือกิน) กลับลำดับ
  เพราะพบว่าความได้เปรียบของ "ลำดับที่ 1 รอบแรก" มาจาก **การได้คุ้ยก่อน** (ถังมีหมาคงที่ ยิ่งคุ้ยความเสี่ยงยิ่งขึ้น
  คนคุ้ยแรกได้การ์ดส่วนใหญ่ของถัง) — กลับเฉพาะลำดับกินไม่มีผลเลย

## 027 — ใช้ S + Xt เป็นกติกาจริง ลบ M
วันที่: 2026-09-25 (เฟส 2 รอบสุดท้าย)
- **S** ลำดับตัดสินเสมอ รอบ 1 สุ่ม · รอบ 2+ แต้มน้อยก่อน (แต้มเท่ากันใช้การสุ่มของรอบนั้น) — ลดคนจบ 0 แต้ม (4 คน 9.7 → 7.7%)
- **Xt** เลขเท่ากัน: เลือกของตามลำดับตัดสินเสมอ แต่ **คุ้ยกลับลำดับ** · กินตามลำดับปกติ
  เหตุผล: ความได้เปรียบของ "ลำดับที่ 1 รอบแรก" (4 คนชนะ 30% จาก 25%) มาจากการได้คุ้ยก่อนเกือบทั้งหมด
  (ถังมีหมาคงที่ ยิ่งคุ้ยยิ่งเสี่ยง) — ทดสอบแยกแล้ว: กลับลำดับกินไม่มีผล กลับลำดับคุ้ยได้ผลเท่ากลับทั้งคู่
- **ยอมรับว่า Xt ทำให้ลำดับที่ 1 รอบแรกเสียเปรียบเล็กน้อยที่ 4 คน** (ตอนตัดสิน: 20% จาก 25% ≈ −5 จุด)
  เพราะเป็นทิศทาง "ช่วยคนตามหลัง" ตามธีมเกม ดีกว่าให้โชคจากการสุ่มรอบแรกได้เปรียบ (หลังใช้ P เหลือ 22.4% ≈ −2.6 จุด)
- **ลบ M** (ช่วงเลขตามจำนวนผู้เล่น): ลดการชนได้น้อย (4 คน 64 → 57% ต่อคน) และไม่เพิ่มมื้อ เพราะหลังมี E
  ทุกคนได้การ์ดจากแผงเท่ากันไม่ว่าชนหรือไม่ · engine กลับไปบังคับ `meowValues` = จำนวนรอบพอดี
- เขียน "ฉบับผู้เล่น" เรื่องลำดับในแต่ละช่วงไว้ที่ GAME_RULES §11 สำหรับหน้าวิธีเล่นและบทสอน

## 028 — P: จำนวนอาหารต่อชนิดตามจำนวนผู้เล่น 8 / 9 / 10 (กติกาจริง)
- ผู้ใช้ตั้งสมมติฐาน: แผงตลาดที่ใหญ่ขึ้นดึงอาหารออกจากถัง ถังที่ 4 คนเล็กลงมากแต่หมาเท่าเดิม
- ตรวจแล้วถูก: อาหารในถังตอนเริ่ม 27 / 22 / 17 ใบ (2/3/4 คน) · ความเสี่ยงเฉลี่ยต่อการคุ้ย 16.4 / 19.2 / 21.5%
- 8/9/10 ต่อชนิดทำให้ถังเริ่มที่ 27 ใบเท่ากันทุกจำนวนผู้เล่น (แผงกิน 15/20/25 ใบ: 42−15 = 47−20 = 52−25 = 27)
- ผล (1,000 เกม/ไลน์อัป): มื้อ 4 คน 1.56 → **2.21** · 3 คน 2.22 → 2.60 · 0 แต้ม ≤ 6.3% · ความเสี่ยงคุ้ย 4 คน 18.2%
  คนไม่ชนยังได้เปรียบ (+6% / +10%) · บอทห่างสุด ≤ 5.7 จุด → ผ่านกฎตัดสินของผู้ใช้ (มื้อ 4 คน ≥ 1.7) จึงใช้โดยไม่ต้องปรับบอท
- `GameConfig.foodCopies` เป็น map ตามจำนวนผู้เล่น

## 029 — ค่าสมดุลทั้งหมดอยู่ใน `config.ts` ไฟล์เดียว
- รอบปรับสมดุลด้วยการจำลองจบแล้ว ส่วนที่เหลือจะดูจากการทดลองเล่นจริง
- ย้าย `BOT_TUNING` และ `EASY_MISTAKE_RATE` จาก `bots/tuning.ts` มาไว้ใน `config.ts` คู่กับ `DEFAULT_CONFIG`
- ลบสวิตช์ทดลองทั้งหมดออกจาก engine · สคริปต์ `balance.ts` มีช่อง `VARIANTS` สำหรับลองค่าใหม่ในอนาคต

## 030 — โครงหน้าเว็บเฟส 3 (โหมดเดี่ยว)
วันที่: 2026-09-25
- **ไม่ใช้ router และยังไม่ใช้ Zustand**: 4 หน้าสลับด้วย state เดียวใน `App.tsx` · หน้าเกมอ่านข้อมูลผ่าน
  `useSyncExternalStore(controller.subscribe, controller.getSnapshot)` — พอสำหรับตอนนี้ ค่อยเพิ่ม router ตอนทำ `/room/ABCD` (เฟส 6)
- **`GameController`** (`getSnapshot`, `subscribe`, `dispatch`, `dispose`) — `LocalController` รัน engine + บอทในเบราว์เซอร์
  หน้าเกมเห็นแค่ `getPlayerView` ของที่นั่งตัวเอง · ตัวจับเวลาบอท (600–1200ms) ฉีดเข้าได้ (`Scheduler`) test จึงเล่นทั้งเกมได้ทันที
- **บันทึกเกม**: localStorage คีย์ `mnm.solo.v1` หลังทุกแอคชัน เก็บ state + ที่นั่ง + สถานะ RNG ของบอท (เล่นต่อได้แบบเดิมเป๊ะ)
  ตรวจรูปแบบก่อนโหลด ถ้าเสียหรือไม่ใช่เวอร์ชันนี้ก็ทิ้ง · ลบเมื่อเกมจบ · ตั้งค่าเกมล่าสุดจำไว้ที่ `mnm.setup.v1`
- **เลขเหมียวใบสุดท้าย** ตัวควบคุมลงให้อัตโนมัติ (ไม่มีทางเลือกอื่น) · ช่วงกินใช้ปุ่ม "กิน{{อาหาร}} +แต้ม" ต่อชนิดแทนการเลือกการ์ดเอง
  (ถ้ามีทั้งแบบใช้และไม่ใช้ปลาทอง แสดงแบบไม่ใช้ปลาทองก่อน ประหยัดตัวแทน)
- **บอทนิสัยซ้ำกันได้เลขต่อท้าย** ("แมวขาวขี้ระวัง 1/2") · ปุ่มเพิ่มคู่แข่งเลือกนิสัยที่ยังไม่มีในโต๊ะก่อน
- **เลย์เอาต์มือถือ 375×812 ใส่ได้ในจอเดียว**: แผงตลาด 5 ช่องแถวเดียว, ป้ายราคาแถบบาง, คู่แข่งแถวเดียว
  (3 คนใช้การ์ดแบบซ้อนชื่อไว้บรรทัดล่าง), แผงของเราติดขอบล่าง (sticky) ปุ่มแอคชันเห็นตลอด · แตะคู่แข่งดูมือ + มื้อที่กิน
- **เพิ่ม token `--alert`** (#ff9e9b) สำหรับข้อความเตือนบนพื้นมืด เพราะ `--shrimp`/`--danger` บนพื้น night คอนทราสต์ไม่ถึง 4.5:1
- ปุ่มที่กดไม่ได้ยังแตะได้ และขึ้นเหตุผล (เช่น "ในถังเหลือแต่หมา…") ตาม ART_DIRECTION
- ยังไม่มีแอนิเมชัน (Framer Motion) และหน้าเดสก์ท็อปเป็นคอลัมน์กลางจอ — เป็นงานเฟส 4

## 031 — "เวที" เล่นเหตุการณ์ทีละจังหวะ (แก้ข้อความผ่านไปเร็วจนอ่านไม่ทัน)
วันที่: 2026-09-25 (ต้นเฟส 4 — ผู้ใช้แจ้งปัญหา)
- เหตุการณ์จาก engine ถูกแปลงเป็น "beat" (`game/stage.ts`) แต่ละอันมีเวลาอ่าน 0.65–1.7 วินาที (ชนกัน +0.9) เล่นทีละอัน
  ระหว่างนั้น **เกมหยุดรอ** (`GameController.setPaused`) บอทไม่ขยับจนกว่าจะแสดงครบ · แตะ/Enter/Esc = ข้าม
- beat สำคัญ (ต้นรอบ, เปิดเลข/ชนกัน, หมาโผล่, โดนไล่, โยนกระดูก, กิน, ข้ามตา, ทิ้งการ์ด, จบเกม) บังกระดานและต้องดู
  ส่วนการหยิบ/คุ้ย/เก็บถุงเป็นป้ายเล็กไม่บังกระดาน · การกระทำของเราเองแสดงสั้น (0.45 วิ) และไม่ขวางการกดครั้งต่อไป
  (ครั้งแรกทำให้ทุกการคุ้ยของบอทบังจอ ช้าเกินไป บอทคุ้ย 6 ใบใช้ ~10 วินาที จึงเปลี่ยนเป็นป้าย)
- ไม่เล่นประวัติย้อนหลังตอนเปิดเกม (นับ event ด้วย `eventCount`) แต่เกมใหม่/เล่นต่อจะเปิดด้วยป้าย "รอบที่ N" + ลำดับตัดสินเสมอ
- แอนิเมชันใช้ `motion` (Framer Motion ชื่อใหม่) · `prefers-reduced-motion` = ไม่มีการเคลื่อนไหว แต่ **เวลาอ่านเท่าเดิม**

## 032 — ถังขยะบอกความเสี่ยงด้วยอารมณ์ แตะดูตัวเลข (ผู้ใช้เลือก)
สงบ (< 15%) · ได้ยินเสียงแปลกๆ (15–25%, เหงื่อตก ถังโยกเบาๆ) · หมาอยู่แถวนี้ (> 25%, ตาหมาเรืองในถัง) · เหลือแต่หมา (หูหมาโผล่)
แตะที่ถังแสดง "เหลือ N ใบ · หมา M ตัว" และ "โอกาสเจอหมา X%"

## 033 — บทสอนใช้กติกาจริง + seed ที่หามาแล้ว (ไม่แก้กติกาเพื่อบทสอน)
- เกม 2 คนกับแมวส้มตะกละ บอทเดินตามสคริปต์ช่วงบทสอน ผู้เล่นทำได้เฉพาะสิ่งที่ป้าเหมียวบอก (อย่างอื่นขึ้น "ลองทำตามที่ป้าบอกก่อนนะ")
- "เซ็ตการ์ดตายตัว": engine รับ `marketTop` (การ์ดบนสุดของกองแผง) แต่ถังขยะถูกสับใหม่ตอนเริ่มช่วงคุ้ย (กติกา D)
  จึงใช้สคริปต์ `apps/web/scripts/find-tutorial-seed.ts` หา seed ที่ทำให้ผู้เล่นคุ้ยได้ ปลา → กระดูก → ปลา → หมา
  ได้ `tutorial-5180` · มี test เล่นบทสอนครบทุกขั้น ถ้ากติกาเปลี่ยนแล้วลำดับเพี้ยน test จะพังและต้องหา seed ใหม่
- หลังขั้นสุดท้ายเล่นต่อเป็นเกมปกติได้ · บทสอนไม่บันทึกทับเกมที่เซฟไว้ · หน้าแรกแนะนำบทสอนจนกว่าจะเคยเปิด (`mnm.tutorialSeen`)

## 034 — โต๊ะบนจอคอม (≥ 1024px) — ทำทีหลังสุดตามลำดับของผู้ใช้
3 คอลัมน์: ซ้าย = คู่แข่ง (มือเปิด + มื้อที่กิน) · กลาง = โต๊ะ (แผง ถัง มือเรา ไม่ติดขอบล่าง) · ขวา = ประวัติเหตุการณ์ทั้งหมด
มือถือ/แท็บเล็ตยังเป็นแบบเดิมทุกอย่าง

## 035 — เล่นเครื่องเดียว: ส่งเครื่องทุกครั้งที่คนต้องเล่นเปลี่ยน
วันที่: 2026-09-25 (เฟส 5)
- จอแสดง view ของ "คนถือเครื่อง" คนเดียวเสมอ (`getPlayerView`) — เมื่อคนที่เกมรอเป็นคนอื่น จอบังทึบ "ส่งเครื่องให้ {{name}}"
  แล้วต้องกด "ฉันคือ {{name}} พร้อมแล้ว" ก่อน จึงสลับ view
- **ช่วงลับ** (ลงเลข, เลือกทิ้งการ์ดมือเกิน) บังจอทันทีหลังคนก่อนกด · **ช่วงเปิด** (หยิบ คุ้ย กิน — มือเป็นข้อมูลเปิดแล้ว)
  ก็บังเหมือนกัน แต่รอให้เหตุการณ์บนจอแสดงครบก่อน (เช่น ให้ทุกคนดูเปิดเลข/ชนกันจบก่อนส่งเครื่อง)
  เหตุผล: ความลับไม่ได้อยู่ที่ช่วงเปิด แต่หน้าบังช่วยให้รู้ว่าต้องส่งเครื่องให้ใคร และกันคนผิดกดแทน
- บอทเล่นอยู่ = เครื่องอยู่กับคนเดิม ไม่บัง · ใบเหมียวใบสุดท้ายแอปลงให้ทุกคนอัตโนมัติ (รอบ 5 ไม่ต้องส่งเครื่องตอนลงเลข)
- เกมใหม่และเกมที่เปิดต่อ เริ่มที่หน้าบังเสมอ (คนถือเครื่องอาจไม่ใช่คนเดิม) และเหตุการณ์ที่ค้างอยู่จะรอจนเปิดหน้าบัง
- ปุ่ม "พร้อมแล้ว" กดได้หลัง 0.7 วินาที กันการแตะของคนก่อนหลุดผ่านหน้าบัง
- ต้องมีคนอย่างน้อย 2 คน (คนเดียว = โหมดเล่นคนเดียว) ที่เหลือเติมบอทได้ รวม 2–4 ที่นั่ง · คนไม่ตั้งชื่อเรียก "ผู้เล่น N"
- จอที่ใช้ร่วมกันเรียกทุกคนด้วยชื่อ (ไม่ใช้ "เธอ" ในประวัติ ลำดับตัดสินเสมอ เหตุการณ์ และผลเกม)
  ยกเว้นแผงของคนถือเครื่องที่ยังบอก "ตาเธอ" เพราะคนถือคือคนที่ต้องกด
- เซฟช่องเดียวกับโหมดเดี่ยว (เริ่มเกมใหม่โหมดไหนก็แทนที่เกมที่ค้าง) หน้าแรกบอกว่าเกมที่ค้างเป็นโหมดไหน
